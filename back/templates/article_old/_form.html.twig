{{ form_start(form, {
    attr: {
        class: 'needs-validation',
        novalidate: 'novalidate'
    }
}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary w-100 py-2 mb-3">
        {{ button_label|default('Enregistrer')|raw }}
    </button>
{{ form_end(form) }}
